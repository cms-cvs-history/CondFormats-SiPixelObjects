process DBTest = {

  untracked PSet maxEvents = {untracked int32 input = 1}

  source = EmptySource {}

  service = MessageLogger { untracked vstring destinations = {"cout"} }

# include "CalibTracker/Configuration/data/SiPixel_FrontierCabling.cfi"
  include "CalibTracker/Configuration/data/SiPixel_SQLiteCabling.cfi"
  replace  siPixelCabling.connect="sqlite_file:cabling.db"
  
  module readstruct =  SiPixelFedCablingMapAnalyzer{}

  #provide a scheduler path
  path p1 = {readstruct}      
}

